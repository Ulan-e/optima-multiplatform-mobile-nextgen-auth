// add "stage" params
PRODUCT_BUNDLE_IDENTIFIER = kg.optima.mobile.stage
PROVISIONING_PROFILE_SPECIFIER = $(sigh_kg.optima.mobile.stage_development_profile-name)
// $() is hack to pass // in value
SERVER_ENDPOINT = https:/$()/stage.localhost/
BUNDLE_DISPLAY_NAME = MOKO app (S)
