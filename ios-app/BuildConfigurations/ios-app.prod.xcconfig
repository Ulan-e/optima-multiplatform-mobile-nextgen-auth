// add "prod" params
PRODUCT_BUNDLE_IDENTIFIER = kg.optima.mobile
PROVISIONING_PROFILE_SPECIFIER = $(sigh_kg.optima.mobile_development_profile-name)
// $() is hack to pass // in value
SERVER_ENDPOINT = https:/$()/localhost/
BUNDLE_DISPLAY_NAME = MOKO app
